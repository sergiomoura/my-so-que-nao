const path = require('path');

module.exports = {
    "config": path.resolve("database", "config.js"),
    "migrations-path": path.resolve("database", "migrations.js"),
    "seeders-path": path.resolve("database", "seeders")
}